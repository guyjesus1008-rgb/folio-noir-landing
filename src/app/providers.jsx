/**
 * Application Providers
 * Wrapper para todos os providers globais da aplicação
 */
export function AppProviders({ children }) {
  return (
    <>
      {/* Adicionar providers aqui conforme necessário */}
      {/* Exemplo: ThemeProvider, AuthProvider, QueryClientProvider */}
      {children}
    </>
  )
}
